spring:
  application:
    name: gateway
  profiles:
    active: dev

  cloud:
    nacos:
      discovery:
#        namespace: prod
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
#        namespace: public
        group: DEFAULT_GROUP